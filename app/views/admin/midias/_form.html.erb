<%= form_with(model: midia) do |form| %>
  <% if midia.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(midia.errors.count, "error") %> prohibited this midia from being saved:</h2>

      <ul>
        <% midia.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :arquivo, style: "display: block" %>
    <%= form.text_area :arquivo %>
  </div>

  <div>
    <%= form.label :pasta_id, style: "display: block" %>
    <%= form.text_field :pasta_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
